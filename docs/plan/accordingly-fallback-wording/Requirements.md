# Accordingly Fallback Wording Requirements

## REQ-001 — Consistent fallback wording

The routing-only Hook, Skill metadata, and README use `accordingly` instead of `normally` when
describing handling outside Better Plan.

## REQ-002 — No semantic expansion

Planning, coding, and explicit implementation still enter Better Plan; every other request still
follows the user's instructions. No lifecycle or host behavior changes.
