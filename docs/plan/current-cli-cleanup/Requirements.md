# Requirements

## REQ-001 Current-only criterion editing

The active CLI suite must describe only the current `edit-node` criterion-replacement
contract. Deleted syntax must not remain as a compatibility path, negative test, or
recurring gate. Removal is proven once by the implementation Node, after which the
durable suite verifies complete replacement and atomic proof invalidation.
