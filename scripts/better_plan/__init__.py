"""Canonical Better Plan implementation package."""

# The Checkpoints Tree generation. The CLI, the installed plugin manifest, and the
# schema markers move together, so a host can tell which surface it is talking to.
__version__ = "4.0.0"
