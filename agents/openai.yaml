interface:
  display_name: "Better Plan"
  short_description: "Lightweight design-first orchestration"
  default_prompt: "Use the native workflow when maintaining the Better Plan source repository; do not require $better-plan. For planning, coding, or explicit implementation elsewhere, use $better-plan to reuse existing Plans and progressively disclose only the capability path being touched. Otherwise, follow the user's instructions."
