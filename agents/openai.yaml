interface:
  display_name: "Better Plan"
  short_description: "Complete-delivery planning with one Designer and one Reviewer"
  default_prompt: "Use the native workflow when maintaining the Better Plan source repository. Elsewhere, use $better-plan only for large, high-risk, migration, or real multi-Task delivery: consolidate user decisions once, run one direct-write Designer, authorize once, then execute without mid-delivery questions and close through one writable Reviewer."
